# AI Tools

核心工程位于 [`ai-tools-web/`](./ai-tools-web/README.md)（FastAPI 后端 + Vue 前端）。

```bash
# 后端
cd ai-tools-web/ai-tools-backend && python3 -m uvicorn app.main:app --reload

# 前端
cd ai-tools-web/ai-tools-frontend && npm run dev
```
